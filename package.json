{
  "name": "imagen-openrouter-tests",
  "private": true,
  "version": "1.0.0",
  "description": "Regression tests for imagen-openrouter UI (4 bug checks + smoke gen).",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.0"
  }
}
